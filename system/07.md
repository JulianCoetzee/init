<!-- Command that returns the Process ID (PID) for Secure Shell Daemon (SSHD)
SSHD will typically write a PID file when the service runs and the cat command can be used to read it -->
cat /var/run/sshd.pid