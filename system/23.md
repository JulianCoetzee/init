<!-- Assuming as in 22 one can add a functional user,
first, connect to the VM via SSH (ssh -i <ssh_private_key> <username>@<ip_host_machine>) and install a GUI (MATE recommended for Ubuntu) 
secondly, Install SSH-tunneling Remote Desktop Solution (X2GO) both client(home) and server(VM)
connect via client (X2GO uses SSH to connect to VM GUI) -->
<!-- see also  https://www.wekeo.eu/using-gui-on-vm-with-linux -->