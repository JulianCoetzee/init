<!-- the following command will return the status of the SSH service 
SSH = (Secure Shell cryptographic network protocol for operating network services securely over  an unsecured network) -->
/etc/init.d/ssh status